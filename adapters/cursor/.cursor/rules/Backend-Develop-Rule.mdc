---
description: 后端 Java 开发规范（通用）
globs:
  - "src/**/*.java"
  - "**/*Controller.java"
  - "**/*Service.java"
alwaysApply: false
---

# 后端开发规范

实现后端 Java 业务时：

1. Read `harness-foundry/references/context-map.md`（跨模块时）

## 要点

- 分层：controller → service → domain → mapper
- 命名：`XxxController`、`IXxxService`、`XxxServiceImpl`
- 关键路径加日志与中文注释
- 禁止：Controller 写业务、循环 SQL、空 catch、泄露敏感信息

验证命令见 `harness-foundry/artifact-templates/project.verification.md`。
